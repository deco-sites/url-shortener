import Component from "$store/components/ui/CopyButton.tsx";
import type { Props } from "$store/components/ui/CopyButton.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
